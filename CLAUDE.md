# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Current state

This repository is newly initialized and currently contains only a `README.md` (project title `csskintone`) and a `.gitignore`. There is no source code, build tooling, or tests yet.

The `.gitignore` is set up for a Node.js project (it ignores `node_modules/`, `.env`, and `.env.local`), so this is the likely intended stack, but no `package.json` or framework has been chosen yet.

## Notes for future updates

Once the project takes shape, replace this section with:
- Build, lint, test, and run commands (including how to run a single test).
- High-level architecture that spans multiple files and isn't obvious from the directory layout.
